<!-- page content -->
<div class="right_col" role="main">
  <div class="">
    <div class="row">
      <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
          <div class="x_title">
            <h2>Server<small>Asset Details</small></h2>
            <div class="clearfix"></div>
            <a href="<%=new_server_path%>" class="btn btn-primary">New Server Asset</a>
          </div>
          <div class="x_content">
            <table id="datatable-buttons" class="table table-striped table-bordered">
              <thead>
                <tr>
                  <th style="width: 7%;">ID</th>
                  <th>Asset Tag</th>
                  <th>Host Name</th>
                  <th>Make</th>
                  <th>Model</th>
                  <th>Serial Number</th>
                  <th>Mac Address</th>
                </tr>
              </thead>
              <tbody>
                <% @server_details.each do |server| %>
                <tr>
                  <td><%=server.id%></td>
                  <td><%=link_to server.asset_tag, edit_server_path(server.id), {:style=>'text-decoration:underline;'}%></td>
                  <td><%=server.host_name%></td>
                  <td><%=server.make%></td>
                  <td><%=server.model%></td>
                  <td><%=server.serial_number%></td>
                  <td><%=server.mac%></td>
                  <td><%=link_to ' Remove', server_path(server.id), method: 'DELETE', data: {confirm: "Are you sure want to delete this?"},:class => 'fa fa-trash-o'%></td>
                </tr>
                <%end %>
              </tbody>
            </table>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
</div>