<!-- page content -->
<div class="right_col" role="main">
  <div class="">
    <div class="row">
      <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
          <div class="x_title">
            <h2>Printer<small>Asset Details</small></h2>
            <div class="clearfix"></div>
            <a href="<%=new_printer_path%>" class="btn btn-primary">New Printer Asset</a>
          </div>
          <div class="x_content">
            <table id="datatable-buttons" class="table table-striped table-bordered">
              <thead>
                <tr>
                  <th style="width: 7%;">ID</th>
                  <th>Asset Tag</th>
                  <th>Host Name</th>
                  <th>Make</th>
                  <th>Model</th>
                  <th>Serial Number</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <% @printers.each do |printer| %>
                <tr>
                  <td><%=printer.id%></td>
                  <td><%=link_to printer.asset_tag, edit_printer_path(printer.id), {:style=>'text-decoration:underline;'}%></td>
                  <td><%=printer.host_name%></td>
                  <td><%=printer.make%></td>
                  <td><%=printer.model%></td>
                  <td><%=printer.serial_number%></td>
                  <td><%=link_to ' Remove', printer_path(printer.id), method: 'DELETE', data: {confirm: "Are you sure want to delete this?"},:class => 'fa fa-trash-o'%></td>
                </tr>
                <%end %>
              </tbody>
            </table>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
</div>